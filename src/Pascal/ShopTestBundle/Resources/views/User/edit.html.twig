{% extends '::base.html.twig' %}

{% block body -%}
    <h1>User edit</h1>

    {{ form(edit_form) }}

    <div class = "role_instructions">
        <p> To Make Admin: <strong> 0|ROLE_ADMIN -- 1|ROLE_USER </strong> </p>
        <p> To Make Normal: <strong> 0|NORMAL -- 1|ROLE_USER </strong> </p>
    </div>

<ul class="record_actions">
    <li>
        <a href="{{ path('user') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% if is_granted("ROLE_ADMIN") %}
    <a href = "{{ path('product_viewAdmin') }}">Admin View</a>
{% endif %}
{% endblock %}
