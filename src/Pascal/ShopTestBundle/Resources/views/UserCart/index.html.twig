{% extends '::base.html.twig' %}

{% block body -%}
    <h1>UserCart list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Timestamp</th>
                <th>Submitted</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('usercart_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
               {# <td>{{ entity.timestamp }}</td>  #}
                <td>{{ entity.submitted }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('usercart_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('usercart_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('usercart_new') }}">
                Create a new entry
            </a>
        </li>
        <li>
            <a href = "{{ path('product_viewAdmin') }}">
                Admin View
            </a>
        </li>
    </ul>
    
    {% endblock %}
